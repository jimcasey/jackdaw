---
name: slice-5-location-spec
description: Slice 5 (Location context, in-app precise GPS) spec decisions — Note location fields (nullable coords, no persisted pending/denied enum), the async backfill honoring the seam contract via a mockable LocationProviding protocol, When-In-Use precise permission + Info.plist, geocode-at-display, editor location row.
metadata:
  type: project
---

**Slice 5 spec = `docs/slices/slice-5-location.md`** (tech-lead, 2026-07-14). The funnel's signature auto-context. **IN-APP precise GPS ONLY** — external capture deferred (ADR 0005), so NO external-location dimension. In-app capture is FOREGROUND → When-In-Use precise GPS works normally (unlike the no-launch App-Intent case that failed the feasibility gate). Owner-settled: precise (not coarse); context = time + location only. Prereq: Triage done (27 tests). Numbering runs one ahead of build-order table; precedes the export slices.

**1. Note location fields (additive/optional → lightweight migration):** `latitude: Double?`, `longitude: Double?`, `horizontalAccuracy: Double?` (metres; distinguishes precise vs reduced, travels to frontmatter), `placeName: String?` (geocoded lazily AT DISPLAY, nil otherwise), computed `var hasLocation: Bool`. **DECISION: do NOT persist a pending/denied location-state enum** — teaching point: "pending" is TRANSIENT (lives in the location task's memory for a few seconds), and a persisted pending flag goes STALE (app killed pre-fix → forever-pending). So `hasLocation==false` = "No location" terminally (denied / never granted / lost to early kill are indistinguishable and don't need to be). Keep `Note.swift` free of the CoreLocation import (no CLLocationCoordinate2D on the @Model).

**2. Async backfill honoring the seam contract:** mockable `protocol LocationProviding { func currentFix() async -> LocationFix?; var authorizationStatus; requestWhenInUseAuthorization(); prewarm() }` (plain, no CoreLocation → tests inject a mock; only concrete `CoreLocationProvider` imports CoreLocation). `struct LocationFix { latitude, longitude, horizontalAccuracy: Double }`. `makeNote` UNCHANGED (external reuses it w/o location). Add `CaptureService.attachLocation(to:in:)` = fire-and-forget `Task { @MainActor in await resolveLocation(...) }`; the awaitable core `@MainActor func resolveLocation(for:using:in:) async` is what tests await; pure `apply(_ fix:to:)` sets fields or leaves nil. **GUARD: check `note.modelContext != nil` before writing** — a note created on first keystroke then pruned (cleared+abandoned) is deleted; its in-flight fix must NOT resurrect a dead object. Wired in `CaptureViewModel.edit` draft==nil branch: `makeNote` then `attachLocation(to: note)` — Task holds strong ref to `note` so the fix lands AFTER `finishEditing` sets draft=nil (satisfies "even after draft detaches"). @MainActor because mainContext is main-actor-bound. Provider CACHES the most recent pre-warmed fix so rapid multi-capture (3-in-a-row) doesn't cold-spin GPS 3×. Capture NEVER awaits inline.

**One-shot API (verified 2026-07-14): use `CLLocationManager` + `startUpdatingLocation`** (pre-warm on sheet appear, `desiredAccuracy = kCLLocationAccuracyBest`, cache latest, stop on dismiss). NOT `requestLocation()` (slow cold ~10s) or raw `CLLocationUpdate.liveUpdates` (no desiredAccuracy/distanceFilter; community prefers CLLocationManager for accuracy+reliability). Protocol hides the choice → swappable later.

**3. Permission + Info.plist:** add ONLY `NSLocationWhenInUseUsageDescription` (required or CRASH). **Precise needs NO separate entitlement** — iOS 14+ When-In-Use prompt has a "Precise: On" toggle (default on); request When-In-Use + set desiredAccuracy=best. Level = When-In-Use (NOT Always — capture is always foreground). Add `NSLocationTemporaryUsageDescriptionDictionary` ONLY IF the reduced-accuracy nudge (§7) is adopted. **Priming (design owns exact moment; we own mechanism):** never ask on launch, never block first capture; once-only gate (`hasPrimedLocation` in UserDefaults) shows a priming sheet BEFORE the system dialog at a non-interrupting moment (first note banked / sheet dismiss, not mid-keystroke) → Continue → requestWhenInUseAuthorization. Note banked pre-grant = timestamp-only (honest). **Denied/restricted → currentFix() nil → timestamp-only, NO error/nag; surface once in Settings sheet (later slice). Reduced accuracy → apply the coarse fix normally** (horizontalAccuracy records it); nudge is optional (§7).

**4. Reverse geocoding: NOT at capture (offline-first sacred — CLGeocoder is network). Lazy AT DISPLAY in the editor, cached into placeName.** Coordinates are the real data (+ frontmatter); name is legibility sugar; map thumbnail renders from coords alone (no name needed). Lean INCLUDE lazy names (design wants them, cheap at display) but MOST CUTTABLE — coords+map only is fine.

**5. Editor location row (replace the `// location row` hook in NoteEditorView):** hasLocation → small STATIC map thumbnail (non-interactive Map or MKMapSnapshotter) + placeName (if resolved) + Clear button (nils the fields + save). !hasLocation → "No location" (no broken map). A11y: map MUST have `.accessibilityLabel` (place name else "Location: lat, lon"); Clear ≥44pt. Ships: row + Clear (+ lazy name if adopted). DEFER: "drop a pin to correct" (design cut-candidate). Editing stays light — no pin-drop/search.

**6. Testing:** off-device in-memory container + mock LocationProviding; await `resolveLocation` (deterministic, no GPS/timers/Date()): backfill lands on right note (other untouched); denied→nil→timestamp-only; pending→resolved (hasLocation false→true); pruned-note guard (delete then resolve → no crash/write); reduced-accuracy applied; placeName set on geocode / nil on fail; `apply` purity direct. Simulator: real prompt (priming→system; grant/deny/Precise-Off), simulated location (Xcode Features→Location / GPX), editor map renders / "No location" / Clear.

**OWNER DECISIONS PENDING (deferred earlier):** (1) location-priming sheet keep/cut — rec KEEP (one-time, non-blocking, raises grant rate); (2) reduced-accuracy nudge — rec CUT for v1 (product-lead+tech-lead lean; reduced fix still usable; cutting also drops the temporary-accuracy Info.plist key + plumbing); (3) reverse-geocode place names v1 — lean INCLUDE lazy-at-display, most cuttable.

See [[slice-2-spec]] (seam contract + CaptureService/CaptureViewModel), [[slice-4-triage-spec]] (NoteEditorView, Note lifecycle), [[capture-nav-and-external]] (external deferred / GPS feasibility), [[stack-recommendations]] (SwiftData additive migration).
