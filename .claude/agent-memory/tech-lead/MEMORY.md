# Memory Index

- [Obsidian write mechanism decision](decision-obsidian-write.md) — ADR 0001: folder-write via security-scoped bookmark; git-push is the fallback.
- [iOS platform gotchas](ios-gotchas.md) — Verified sandbox/URL-scheme/bookmark facts to avoid re-deriving or misciting.
- [Walking-skeleton build order](build-order.md) — Ratified slice sequence in docs/build-order.md; lazy vault setup, two-tab nav, Talon seam contracts, open owner calls.
- [Stack ADRs 0002/0003](stack-recommendations.md) — Ratified: min iOS target = iOS 26 (ADR 0002), persistence = SwiftData (ADR 0003); load-bearing at Slice 0 / Slice 2.
- [Slice 1 spec](slice-1-spec.md) — Vault bookmark write+verify harness in docs/slices/; Talon seam seed, UserDefaults storage, no-iOS-entitlement fact, T2/T1 PASS/FAIL protocol. PASSED on-device; T2 ratified.
- [Slice 2 spec](slice-2-spec.md) — Thin capture + SwiftData in docs/slices/; Note model, MVVM↔SwiftData, AUTOSAVE-as-you-type (lazy-create/prune-on-abandon), additive migration. Nav flipped to sheet (see below).
- [Capture nav pivot + external capture](capture-nav-and-external.md) — Nav flip to Triage-root + auto Capture sheet; CaptureNoteIntent seam; HARD FINDING: no-launch App Intent can't get precise GPS → external captures timestamp-only. ADR 0004/0005 recommended.
- [Slice 4 triage spec](slice-4-triage-spec.md) — Real Triage inbox in docs/slices/; NoteStatus enum (raw-string storage), calendar-day snooze model, discard-undo (deferred-delete, rec ADOPT), light editing, @Query+VM split.
- [Slice 5 location spec](slice-5-location-spec.md) — In-app precise GPS in docs/slices/; nullable coord fields (no persisted pending enum), mockable LocationProviding + async backfill bound to Note (pruned-note guard), When-In-Use precise + Info.plist, geocode-at-display.
- [Slice 6 Apple Notes export spec](slice-6-apple-notes-spec.md) — Intermediate export milestone (build-order Slice 5) in docs/slices/; **seam evolved to batch/async/per-note-outcome** (SerializedNote/ExportOutcome; ExportFailure String-backed); pure NoteSerializer (YAML frontmatter, deterministic) + RetentionMachine (delete only on confirm) + ExportCoordinator, reused verbatim by Obsidian; AppleNotesDestination = share sheet with DEGRADED confirm. Implemented on `claude/slice-5-ftbwen`, not yet compiled.
- [Slice 7 Obsidian export spec](slice-7-obsidian-spec.md) — v1 feature-complete slice; the whole pipeline (incl. `ObsidianFolderDestination`) reused verbatim from Slice 6 with NO adapter change — it's all acquisition/recovery/surfacing UX, near-zero new logic, high device-only surface. Owner chose **Hybrid** export trigger (auto-export on Keep once a vault exists; picker never fires from the keep swipe) + **counts-only** surfacing. New: `OutboxSummary` classifier, `autoExportKept`, `returnToInbox`, `.fileImporter` setup/re-grant, bottom bar. `PR CI` green ≠ done — v1 bar is an owner on-device checklist (picker, bookmark-across-cold-launch, Sync, re-grant, VoiceOver).
- [Now-playing feasibility + v1.x wave plan](now-playing-and-v1x-wave.md) — PROPOSED (not ratified) 2026-07-23: now-playing verdict (Apple Music in-app YES / background unverified / Apple Podcasts DEAD / system-wide DEAD / Overcast via Shortcuts), context-via-intent-parameters principle, parameter-vs-launcher surface taxonomy, NoteType = fixed raw-string enum, NO App Group needed for launcher-only extensions, proposed slice order S1/A–F.
- [Xcode Cloud CI + TestFlight](xcode-cloud-ci.md) — ADR 0006 (PROPOSED): two phased owner-configured workflows — Phase 1 PR CI (JackdawTests only) → Phase 2 TestFlight on merge. GUARDRAILS: agent never creates/triggers/reconfigs workflows; inner loop stays local; triggers only PR/merge-to-main, never WIP; UI tests local. Quota = 25 compute-h/mo (wall-clock, no rollover); cloud-managed signing.
